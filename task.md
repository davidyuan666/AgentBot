# 任务清单（AgentBot）

请根据下面任务，生成可执行步骤并在 Windows 上依次执行（能执行的就执行，不能执行的给出原因与替代方案），并在执行过程中持续输出进度。

## 任务

1. 获取当前系统信息（CPU/内存/磁盘/进程数），并整理成表格。
2. 在当前目录下创建一个 `output` 文件夹。
3. 在 `output` 目录内写入一个 `result.txt`，内容为第 1 步的系统信息。
4. 截图并把截图文件路径写出来。

## 约束

- 尽量使用 `execute_command` 完成文件操作（创建目录、写文件等）。
- 每一步都要输出：开始/结束/成功或失败/失败原因。

